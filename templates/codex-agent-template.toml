name = "Human-readable agent name"
description = "One sentence describing when Codex should delegate to this agent."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
State the agent's role, inputs, boundaries, and success criteria.

Describe the review or execution protocol in decision-complete terms. Name required repository guidance and expected output structure. Review agents should remain read-only; grant `workspace-write` only when the agent must run checks that generate ordinary build or test artifacts.

List actions the agent must not take, especially source edits, external publication, commits, pushes, or destructive commands.
"""
